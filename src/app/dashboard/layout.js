export const metadata = {
  title: "Wedding Dashboard | FOLDÈ Design",
  description: "Manage your private digital wedding invitation.",
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
