export const metadata = {
  title: "Payment confirmed | FOLDÈ Wedding",
  description: "Confirmation of your FOLDÈ Wedding order.",
  robots: { index: false, follow: false },
};

export default function SuccessLayout({ children }) {
  return children;
}
